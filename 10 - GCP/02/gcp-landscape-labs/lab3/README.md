# Lab 3 — Cloud Run functions kit (supplied complete)
- `hello-function/` — HTTP function, entry point `hello`
- `gcs-function/` — event-driven function, entry point `on_upload`, fires on Cloud Storage object finalized
Nothing to edit. Deploy commands are on the slides / in the lab guide.
lab3.sh: the commands, block by block. teardown.sh: trigger, functions, bucket.
