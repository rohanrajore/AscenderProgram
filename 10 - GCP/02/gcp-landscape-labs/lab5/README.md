# Lab 5 — Cloud SQL Postgres + Firestore kit
lab5.sh: the commands, block by block (Step 0 is the kick-off you run at the start of the storage chapter).
books.sql: what you paste at the psql prompt in step 2.
teardown.sh: deletes the instance and the Firestore database.
memorystore-optional.sh: the optional Memorystore extra (needs a temporary VM).
